---

# - name: Install cronjob
#   become: yes
#   copy:
#     src: etc/cron.d/queerhaus
#     dest: /etc/cron.d/queerhaus

- name: Create codimd directory
  file:
    path: /opt/queerhaus/codimd
    state: directory
    group: queers
    mode: "u=rwx,g=rwx,o=rx,g+s"

# - name: Install backup script
#   copy:
#     src: backup.sh
#     dest: /opt/queerhaus/collab/backup.sh
#     group: queers
#     mode: "u=rwx,g=rwx,o=rx"

- name: Copy docker compose
  copy:
    src: docker-compose.yaml
    dest: /opt/queerhaus/codimd/docker-compose.yaml
    group: queers
    mode: "u=rw,g=rw,o=r"

- name: Template env file
  template:
    src: .env
    dest: /opt/queerhaus/codimd/.env
    group: queers
    mode: "u=rw,g=rw,o=r"

- name: Pull containers images (this will take time)
  command: 
    cmd: docker-compose pull
    chdir: /opt/queerhaus/codimd

- name: Start up containers (this can be slow)
  command: 
    cmd: docker-compose up -d --remove-orphans
    chdir: /opt/queerhaus/codimd
